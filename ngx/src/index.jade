doctype html
html(ng-app="app")
    head
        meta(charset="utf-8")

        //- Display environment when not in production
        if environment === "production"
            title Angular Express
        else
            title= '[' + environment + '] - Angular Express'

        base(href="/")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")

        //- Load jspm loader and configuration
        script(src="_jspm_packages/system.js")
        script(src="config.js")

        //- Try to load bundle if running in production
        if environment === "production"
            script(src="build.js")

        //- Bootstrap the application
        script.
            System.import('app');

    body
        h1 Woohoo!
        p Welcome to your Angular Express application!

        pre.ng-cloak(ng-show="true") Angular bootstrapped successfully!
